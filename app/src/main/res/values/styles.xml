<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <!-- 背景模糊的弹窗效果 -->
    <style name="Custom_Dialog_Theme_Background_DimEnabled_True" parent="android:style/Theme.Dialog">
        <!--背景颜色及和透明程度-->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!--是否去除标题 -->
        <item name="android:windowNoTitle">true</item>
        <!--是否去除边框-->
        <item name="android:windowFrame">@null</item>
        <!--是否浮现在activity之上-->
        <item name="android:windowIsFloating">true</item>
        <!--是否模糊-->
        <item name="android:backgroundDimEnabled">true</item>
    </style>
    <!-- 背景不模糊的弹窗效果 -->
    <style name="Custom_Dialog_Theme_Background_DimEnabled_False" parent="android:style/Theme.Dialog">
        <!--背景颜色及和透明程度-->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!--是否去除标题 -->
        <item name="android:windowNoTitle">true</item>
        <!--是否去除边框-->
        <item name="android:windowFrame">@null</item>
        <!--是否浮现在activity之上-->
        <item name="android:windowIsFloating">true</item>
        <!--是否模糊-->
        <item name="android:backgroundDimEnabled">false</item>
    </style>
    <style name="editpic_textstyle">
        <item name="android:textSize">12dp</item>
    </style>
    <style name="Selectfile_Dialog_style" parent="Custom_Dialog_Theme_Background_DimEnabled_True">
        <item name="android:windowFullscreen">true</item>
    </style>
    <style name="selectfile_textstyle">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:gravity">center</item>
        <item name="android:paddingTop">@dimen/loan_padding_toparent_without_shadow</item>
        <item name="android:paddingBottom">@dimen/loan_padding_toparent_without_shadow</item>
        <item name="android:textColor">@color/loan_title_black</item>
        <item name="android:textSize">@dimen/loan_normal_title_size</item>
    </style>
</resources>
